<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="calculadora.pe.edu.upeu.calculadoraexamen" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/calcu"
        android:label= "@string/menu"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MenuActivity"
            android:label="@string/menu" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:icon="@drawable/logoupeu"
            android:name=".EstandarActivity"
            android:label="@string/btnEstandar">
        </activity>

        <activity
            android:icon="@drawable/logoupeu"
            android:name=".CientificaActivity"
            android:label="@string/btnCientifica">
        </activity>
    </application>

</manifest>
